meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{host}}/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "email": "maarten.thoelen@mighty.be"
  }
}
