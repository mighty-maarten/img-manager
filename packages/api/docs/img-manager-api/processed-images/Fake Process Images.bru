meta {
  name: Fake Process Images
  type: http
  seq: 1
}

post {
  url: {{host}}/processed-images/fake-process
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "labelName": "HR"
  }
}
